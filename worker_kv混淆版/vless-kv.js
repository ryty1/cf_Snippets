const _mn = (() => { const a = 'clo' + 'udf' + 'lar' + 'e', b = 'so' + 'ck' + 'ets'; return a + ':' + b; })(); let _rf; async function _rm(h) { if (!_rf) _rf = (await import(_mn)).connect; return _rf(h); }
const T = 'e4d59882-dbac-426b-ae64-5abed091b6be';
const FA = 'ProxyIP.cmliussss.net';
const FP = '443';
const CU = '';    // 配置面板的域名完整地址，https://xxxx.com
const AS = '4541-ae39-d304267678e3';
const SB = 'https://clash.594880.xyz';
const PW = 'abc123456';
const CFG_URL = CU, CFG_KEY = AS, IS_OWNER = !!CU, CD = 15000, RCT = 3600000; let SC = [], DD = [{ domain: 'cf.090227.xyz' }], cC = null, cT = 0, RC = {};
function genSocksKey(idx) { const src = T + '-socks-' + idx, chars = 'ABCDEFGHJKLMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz23456789'; let hash = 0; for (let i = 0; i < src.length; i++) { hash = ((hash << 5) - hash) + src.charCodeAt(i); hash = hash & hash; } let key = ''; for (let i = 0; i < 8; i++) { hash = Math.abs((hash * 1103515245 + 12345) & 0x7fffffff); key += chars[hash % chars.length]; } return key; }
const E1 = 'err:d01', E2 = 'err:d02', E3 = 'err:d03', E4 = 'err:d04', E5 = 'err:d05', E6 = 'err:d06', E7 = 'err:d07', E8 = 'err:d08', E9 = 'err:d09', EA = 'err:d0a', EB = 'err:d0b', EC = 'err:d0c', ED = 'err:d0d';
const A1 = 1, A2 = 2, A3 = 3;
async function lC(force = false) { if (!CFG_URL) return { socks: SC, domains: DD }; const now = Date.now(); if (!force && cC && (now - cT) < CD) return cC; try { const ac = new AbortController(), to = setTimeout(() => ac.abort('timeout'), 2600); let r; try { r = await fetch(`${CFG_URL}/api/sync?key=${CFG_KEY}`, { headers: { 'User-Agent': 'Mozilla/5.0 (compatible)' }, signal: ac.signal }); } finally { clearTimeout(to); } if (!r.ok) throw new Error(`HTTP ${r.status}`); const data = await r.json(); if (data.socks && Array.isArray(data.socks)) SC = data.socks; if (data.domains && Array.isArray(data.domains)) DD = data.domains; cC = { socks: SC, domains: DD }; cT = now; } catch (e) { } return { socks: SC, domains: DD }; } function pRelay(c) { if (!c || typeof c !== 'string' || !c.startsWith('relay:')) return null; const raw = c.slice(6), i = raw.indexOf('|'); if (i <= 0) return null; try { return { origin: raw.slice(0, i), token: decodeURIComponent(raw.slice(i + 1)) }; } catch (e) { return null; } } async function rCfg(c, idx = -1) { const rp = pRelay(c); if (!rp) return c; const k = rp.origin + '|' + rp.token, now = Date.now(); if (RC[k] && (now - RC[k].t) < RCT) return RC[k].v; const pull = async (x) => { const ac = new AbortController(), to = setTimeout(() => ac.abort('timeout'), 2800); try { const r = await fetch(`${x.origin}/api/socks-relay?token=${encodeURIComponent(x.token)}`, { headers: { 'User-Agent': 'Mozilla/5.0 (compatible)' }, signal: ac.signal }); if (!r.ok) throw new Error(`HTTP ${r.status}`); const d = await r.json(); const v = d && d.config ? String(d.config).trim() : ''; if (!v) throw new Error('empty'); RC[x.origin + '|' + x.token] = { v, t: Date.now() }; return v; } finally { clearTimeout(to); } }; try { return await pull(rp); } catch (e) { if (idx >= 0) { try { await lC(true); const n = SC.find(s => (s.idx !== undefined ? s.idx : SC.indexOf(s)) === idx), nc = n && n.config ? String(n.config) : '', np = pRelay(nc); if (np) return await pull(np); } catch (e2) { } } if (RC[k] && RC[k].v) return RC[k].v; return ''; } }
export default { async fetch(request, env, ctx) { await lC(); try { const url = new URL(request.url), sT = T.split('-')[0], ug = request.headers.get('Upgrade'); if (ug && ug === ['web', 'sock', 'et'].join('')) return await hWs(request); else if (request.method === 'GET') { if (url.pathname === '/') return new Response(gHTML(), { status: 200, headers: { 'Content-Type': 'text/html; charset=utf-8' } }); if (url.pathname === '/api/health') return new Response(JSON.stringify({ status: 'ok', ts: Date.now() }), { headers: { 'Content-Type': 'application/json' } }); if (url.pathname === '/robots.txt') return new Response('User-agent: *\nAllow: /\n', { headers: { 'Content-Type': 'text/plain' } }); if (url.pathname === '/favicon.ico') return new Response(null, { status: 204 }); if (url.pathname === '/api/check-socks') return await hChk(request); if (url.pathname.toLowerCase().includes(`/${sT}`)) return await hSub(request, T); } return new Response('Not Found', { status: 404 }); } catch (err) { return new Response(err.toString(), { status: 500 }); } } };
async function hSub(request, uuid) { const url = new URL(request.url), fL = [], wd = url.hostname, dp = url.searchParams.get('domains'), pp = url.searchParams.get('ports'); const isClash = url.searchParams.has('clash'); if (isClash) { const subUrl = new URL(request.url); subUrl.searchParams.delete('clash'); const cvUrl = `${SB}/sub?url=${encodeURIComponent(subUrl.toString())}&target=clash.meta&emoji=true`; try { const r = await fetch(cvUrl, { headers: { 'User-Agent': 'Mozilla/5.0' } }); const txt = await r.text(); return new Response(txt, { headers: { 'Content-Type': 'text/yaml; charset=utf-8', 'Cache-Control': 'no-store' } }); } catch (e) { return new Response('Convert failed', { status: 500 }); } } let dl = DD; if (dp) { const idxs = dp.split(',').map(s => parseInt(s.trim())).filter(n => !isNaN(n) && n >= 0 && n < DD.length); if (idxs.length > 0) dl = idxs.map(i => DD[i]); } fL.push(...gLinks(dl, uuid, wd, {})); if (fL.length === 0) return new Response('No data available', { status: 404 }); const st = fL.join('\n'), enc = new TextEncoder(), ub = enc.encode(st), sc = btoa(String.fromCharCode(...ub)); return new Response(sc, { headers: { 'Content-Type': 'text/plain; charset=utf-8', 'Cache-Control': 'no-store, no-cache, must-revalidate, max-age=0' } }); }
function gLinks(dl, uuid, wd, ft = {}) { const p = 443, links = [], wp = encodeURIComponent('/?ed=2048'), proto = 'vl' + 'es' + 's'; dl.forEach(item => { if (SC.length === 0) { const dn = `Snip-默认`; const dp = new URLSearchParams(); dp.set('en' + 'crypt' + 'ion', 'none'); dp.set('se' + 'cur' + 'ity', 'tls'); dp.set('ty' + 'pe', 'ws'); dp.set('ho' + 'st', wd); dp.set('pa' + 'th', wp); dp.append('sni', wd); dp.append('fp', 'firefox'); links.push(`${proto}://${uuid}@${item.domain}:${p}?${dp.toString()}#${encodeURIComponent(dn)}`); } SC.forEach(sc => { const si = sc.idx !== undefined ? sc.idx : SC.indexOf(sc), { region, note } = sc, nn = `Snip-${region}-${note || ''}`, wps = encodeURIComponent(`/?ed=2048&socks=${si}&${genSocksKey(si)}`); const pm = new URLSearchParams(); pm.set('en' + 'crypt' + 'ion', 'none'); pm.set('se' + 'cur' + 'ity', 'tls'); pm.set('ty' + 'pe', 'ws'); pm.set('ho' + 'st', wd); pm.set('pa' + 'th', wps); pm.append('sni', wd); pm.append('fp', 'firefox'); links.push(`${proto}://${uuid}@${item.domain}:${p}?${pm.toString()}#${encodeURIComponent(nn)}`); }); }); return links; }
async function hWs(request) { const url = new URL(request.url), sP = url.searchParams.get('socks'), sI = sP !== null ? parseInt(sP) : -1; let pSC = {}, iSE = false; if (sI >= 0) { const scItem = SC.find(s => (s.idx !== undefined ? s.idx : SC.indexOf(s)) === sI); if (!scItem) return new Response('Unauthorized', { status: 401 }); const expKey = genSocksKey(sI), urlKeys = Array.from(url.searchParams.keys()); let hasKey = false; for (const k of urlKeys) { if (k === expKey && url.searchParams.get(k) === '') { hasKey = true; break; } } if (!hasKey) return new Response('Unauthorized', { status: 401 }); let realConfig = scItem.config; if (!realConfig) return new Response('Config not found', { status: 500 }); if (typeof realConfig === 'string' && realConfig.startsWith('relay:')) realConfig = await rCfg(realConfig, sI); if (!realConfig) return new Response('Config not found', { status: 500 }); try { pSC = pS5(realConfig); iSE = true; } catch (e) { return new Response('Invalid config', { status: 500 }); } } const wsPair = new WebSocketPair(), [cS, sS] = Object.values(wsPair); sS.accept(); let rcw = { socket: null }, isDns = false; const _swp = ['sec-', 'web', 'sock', 'et-', 'proto', 'col'].join(''); const ed = request.headers.get(_swp) || '', rd = mRS(sS, ed); rd.pipeTo(new WritableStream({ async write(chunk) { if (isDns) return await fUDP(chunk, sS, null); if (rcw.socket) { const w = rcw.socket.writable.getWriter(); await w.write(chunk); w.releaseLock(); return; } const { hasError, message, addressType, port, hostname, rawIndex, version, isUDP } = pPkt(chunk, T); if (hasError) throw new Error(message); if (isUDP) { if (port === 53) isDns = true; else throw new Error(E4); } const rh = new Uint8Array(2); rh[0] = version[0]; const rD = chunk.slice(rawIndex); if (isDns) return fUDP(rD, sS, rh); await fTCP(addressType, hostname, port, rD, sS, rh, rcw, iSE, pSC); } })).catch((e) => { }); return new Response(null, { status: 101, webSocket: cS }); }
async function fTCP(aT, host, pN, rD, ws, rh, rcw, iSE, pSC) { if (!iSE) { try { const nS = await _rm({ hostname: FA, port: parseInt(FP, 10) || 443 }); await nS.opened; const nW = nS.writable.getWriter(); await nW.write(rD); nW.releaseLock(); rcw.socket = nS; nS.closed.catch(() => { }).finally(() => cSock(ws)); cStr(nS, ws, rh, null); } catch (e) { cSock(ws); } return; } let hasRetried = false; async function rC() { if (hasRetried) return; hasRetried = true; const useProxy = FA && FA.trim(); if (!useProxy) { cSock(ws); return; } try { const nS = await eSocks(aT, FA, parseInt(FP, 10) || 443, pSC); const nW = nS.writable.getWriter(); await nW.write(rD); nW.releaseLock(); rcw.socket = nS; nS.closed.catch(() => { }).finally(() => cSock(ws)); cStr(nS, ws, rh, null); } catch (e) { cSock(ws); } } try { const rS = await eSocks(aT, host, pN, pSC); const w = rS.writable.getWriter(); await w.write(rD); w.releaseLock(); rcw.socket = rS; rS.closed.then(() => { if (ws.readyState === 1) rC(); }).catch(() => { rC(); }); cStr(rS, ws, rh, rC); } catch (e) { try { await rC(); } catch (err) { cSock(ws); } } }
function pPkt(chunk, token) { if (chunk.byteLength < 24) return { hasError: true, message: E1 }; const dv = new DataView(chunk instanceof ArrayBuffer ? chunk : chunk.buffer || chunk); const ver = new Uint8Array(chunk.slice(0, 1)); if (fID(new Uint8Array(chunk.slice(1, 17))) !== token) return { hasError: true, message: E2 }; const oL = new Uint8Array(chunk.slice(17, 18))[0]; const cmdOff = 18 + oL; const cmd = new Uint8Array(chunk.slice(cmdOff, cmdOff + 1))[0]; let isUDP = false; if (cmd === 1) { } else if (cmd === 2) { isUDP = true; } else return { hasError: true, message: E3 }; const pI = cmdOff + 1, port = new DataView(chunk.slice(pI, pI + 2)).getUint16(0); let aI = pI + 2, aL = 0, aVI = aI + 1, hn = ''; const aT = new Uint8Array(chunk.slice(aI, aVI))[0]; switch (aT) { case A1: aL = 4; hn = new Uint8Array(chunk.slice(aVI, aVI + aL)).join('.'); break; case A2: aL = new Uint8Array(chunk.slice(aVI, aVI + 1))[0]; aVI += 1; hn = new TextDecoder().decode(chunk.slice(aVI, aVI + aL)); break; case A3: aL = 16; const ipv6 = [], iv = new DataView(chunk.slice(aVI, aVI + aL)); for (let i = 0; i < 8; i++) ipv6.push(iv.getUint16(i * 2).toString(16)); hn = ipv6.join(':'); break; default: return { hasError: true, message: `${E5}: ${aT}` }; } if (!hn) return { hasError: true, message: `${E6}: ${aT}` }; return { hasError: false, addressType: aT, port, hostname: hn, isUDP, rawIndex: aVI + aL, version: ver }; }
function mRS(socket, edH) { let c = false; return new ReadableStream({ start(ctrl) { socket.addEventListener('message', (e) => { if (!c) ctrl.enqueue(e.data); }); socket.addEventListener('close', () => { if (!c) { cSock(socket); ctrl.close(); } }); socket.addEventListener('error', (e) => ctrl.error(e)); const { earlyData, error } = b64A(edH); if (error) ctrl.error(error); else if (earlyData) ctrl.enqueue(earlyData); }, cancel() { c = true; cSock(socket); } }); }
async function cStr(rS, wS, hD, rF) { let h = hD, hsDt = false; await rS.readable.pipeTo(new WritableStream({ async write(chunk, ctrl) { hsDt = true; if (wS.readyState !== 1) ctrl.error(E7); if (h) { wS.send(await new Blob([h, chunk]).arrayBuffer()); h = null; } else wS.send(chunk); }, abort(r) { } })).catch(async (e) => { if (rF) { try { await rF(); } catch (err) { cSock(wS); } } else { cSock(wS); } }); if (!hsDt && rF) { try { await rF(); } catch (e) { cSock(wS); } } }
async function fUDP(uC, wS, rH) { try { const _dh = [8, 8, 4, 4].join('.'), _dp = 50 + 3; const tS = await _rm({ hostname: _dh, port: _dp }); let vH = rH; const w = tS.writable.getWriter(); await w.write(uC); w.releaseLock(); await tS.readable.pipeTo(new WritableStream({ async write(chunk) { if (wS.readyState === 1) { if (vH) { wS.send(await new Blob([vH, chunk]).arrayBuffer()); vH = null; } else wS.send(chunk); } } })); } catch (e) { } }
async function eSocks(aT, addr, port, pSC) { const { username, password, hostname, socksPort } = pSC; const s = await _rm({ hostname, port: socksPort }); const w = s.writable.getWriter(); const _sv = 3 + 2; await w.write(new Uint8Array(username ? [_sv, 2, 0, 2] : [_sv, 1, 0])); const r = s.readable.getReader(); let res = (await r.read()).value; if (res[0] !== _sv || res[1] === 255) throw new Error(EA); if (res[1] === 2) { if (!username || !password) throw new Error(EB); const enc = new TextEncoder(), aR = new Uint8Array([1, username.length, ...enc.encode(username), password.length, ...enc.encode(password)]); await w.write(aR); res = (await r.read()).value; if (res[0] !== 1 || res[1] !== 0) throw new Error(EC); } const enc = new TextEncoder(); let DA; switch (aT) { case A1: DA = new Uint8Array([1, ...addr.split('.').map(Number)]); break; case A2: DA = new Uint8Array([3, addr.length, ...enc.encode(addr)]); break; case A3: DA = new Uint8Array([4, ...addr.split(':').flatMap(x => [parseInt(x.slice(0, 2), 16), parseInt(x.slice(2), 16)])]); break; default: throw new Error(E5); } await w.write(new Uint8Array([_sv, 1, 0, ...DA, port >> 8, port & 255])); res = (await r.read()).value; if (res[1] !== 0) throw new Error(ED); w.releaseLock(); r.releaseLock(); return s; }
function pS5(s) { let u = '', p = '', h, pt; const ai = s.lastIndexOf('@'); if (ai > 0) { const ap = s.substring(0, ai), hp = s.substring(ai + 1), ci = ap.indexOf(':'); if (ci > 0) { u = ap.substring(0, ci); p = ap.substring(ci + 1); } const lc = hp.lastIndexOf(':'); if (lc > 0) { h = hp.substring(0, lc); pt = Number(hp.substring(lc + 1)); } else { h = hp; pt = 1080; } } else { const lc = s.lastIndexOf(':'); if (lc > 0) { h = s.substring(0, lc); pt = Number(s.substring(lc + 1)); } else { h = s; pt = 1080; } } if (isNaN(pt)) throw new Error(E9); return { username: u, password: p, hostname: h, socksPort: pt }; }
function b64A(b) { if (!b) return { error: null }; try { b = b.replace(/-/g, '+').replace(/_/g, '/'); return { earlyData: Uint8Array.from(atob(b), (c) => c.charCodeAt(0)).buffer, error: null }; } catch (e) { return { error: e }; } }
function isVF(uuid) { return /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(uuid); }
function cSock(s) { try { if (s.readyState === 1 || s.readyState === 2) s.close(); } catch (e) { } }
const hT = Array.from({ length: 256 }, (v, i) => (i + 256).toString(16).slice(1));
function fID(arr, o = 0) { const id = (hT[arr[o]] + hT[arr[o + 1]] + hT[arr[o + 2]] + hT[arr[o + 3]] + "-" + hT[arr[o + 4]] + hT[arr[o + 5]] + "-" + hT[arr[o + 6]] + hT[arr[o + 7]] + "-" + hT[arr[o + 8]] + hT[arr[o + 9]] + "-" + hT[arr[o + 10]] + hT[arr[o + 11]] + hT[arr[o + 12]] + hT[arr[o + 13]] + hT[arr[o + 14]] + hT[arr[o + 15]]).toLowerCase(); if (!isVF(id)) throw new TypeError(E8); return id; }
async function hChk(request) { if (!IS_OWNER) { return new Response(JSON.stringify({ error: '分享模式不支持检测', message: '分享配置时不支持有效性检测' }), { headers: { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*' } }); } const url = new URL(request.url), idx = url.searchParams.get('index'), chkOne = async (sc) => { const st = Date.now(); try { if (sc && sc.shared) return { region: sc.region, status: 'shared', latency: -1, error: '分享节点不检测' }; const raw0 = sc && sc.config ? String(sc.config).trim() : ''; if (!raw0) return { region: sc && sc.region ? sc.region : 'unknown', status: 'offline', latency: -1, error: '无配置' }; const raw = (typeof raw0 === 'string' && raw0.startsWith('relay:')) ? await rCfg(raw0, (sc && Number.isInteger(sc.idx)) ? sc.idx : -1) : raw0; if (!raw) return { region: sc && sc.region ? sc.region : 'unknown', status: 'offline', latency: -1, error: '中转获取失败' }; const ps = pS5(raw); const tryConn = async (aT, host) => { const tp = new Promise((_, rej) => setTimeout(() => rej(new Error('timeout')), 6500)); const cp = eSocks(aT, host, 80, ps); return await Promise.race([cp, tp]); }; let s; try { s = await tryConn(A2, 'cloudflare.com'); } catch (e1) { s = await tryConn(A1, '1.1.1.1'); } const lt = Date.now() - st; try { s.close(); } catch (e) { } return { region: sc.region, status: 'online', latency: lt }; } catch (e) { return { region: sc && sc.region ? sc.region : 'unknown', status: 'offline', latency: -1, error: e.message || 'failed' }; } }; if (idx !== null) { const i = parseInt(idx); if (i >= 0 && i < SC.length) { const r = await chkOne(SC[i]); return new Response(JSON.stringify(r), { headers: { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*' } }); } } const rs = []; for (const sc of SC) rs.push(await chkOne(sc)); return new Response(JSON.stringify({ results: rs }), { headers: { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*' } }); }
function gHTML() { const np = !!(PW && PW.trim()), pv = PW || '', isOwner = IS_OWNER, _l1 = 'V2' + 'ray' + 'N', _l2 = 'Cl' + 'as' + 'h', LS = SC.filter(s => !s.shared), SS = SC.filter(s => s.shared); return `<!DOCTYPE html><html><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Snip Hub</title><style>*{margin:0;padding:0;box-sizing:border-box}:root{--bg:#0d1117;--card:#161b22;--border:#30363d;--text:#e6edf3;--muted:#8b949e;--pri:#238636;--prih:#2ea043;--dan:#da3633;--danh:#f85149;--acc:#58a6ff}body{font-family:system-ui,-apple-system,sans-serif;background:var(--bg);color:var(--text);min-height:100vh;padding:8px;display:flex;align-items:flex-start;justify-content:center;zoom:0.9}.co{max-width:1280px;width:100%;background:var(--card);border:1px solid var(--border);border-radius:10px}#lo.co{max-width:400px;margin:auto}.h{background:var(--card);color:var(--text);padding:20px;text-align:center;position:relative;border-bottom:1px solid var(--border)}.h h2{font-size:22px;margin-bottom:8px;background:linear-gradient(135deg,#58a6ff,#a371f7);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}.h p{font-size:13px;color:var(--muted)}.h .logout{position:absolute;top:20px;right:20px;color:var(--text);background:transparent;border:1px solid var(--border);padding:6px 12px;border-radius:6px;cursor:pointer;font-size:12px}.h .logout:hover{border-color:var(--acc);color:var(--acc)}.s{padding:8px 16px;border:1px solid var(--border);border-radius:8px;background:var(--bg)}#ff{padding:16px;display:flex;flex-direction:column;gap:10px}.t{font-size:14px;font-weight:600;margin-bottom:0;color:var(--text);display:flex;align-items:center;cursor:pointer;user-select:none;gap:6px}.t::before{content:'';display:inline-block;width:4px;height:16px;background:linear-gradient(180deg,#58a6ff,#a371f7);margin-right:8px;border-radius:2px}.t .arrow{transition:transform .3s;display:inline-block;color:var(--muted);font-size:12px}.t .arrow.open{transform:rotate(90deg)}.t .btn-small:first-of-type{margin-left:auto}.fold-content{max-height:0;overflow:hidden;transition:max-height .3s ease-out}.fold-content.open{max-height:2000px}.btn-group{display:flex;gap:6px}.btn-small{padding:4px 8px;border:1px solid var(--border);border-radius:6px;background:transparent;color:var(--text);font-size:11px;cursor:pointer}.btn-small:hover{border-color:var(--acc);color:var(--acc)}.dg{display:grid;grid-template-columns:repeat(2,1fr);gap:6px;margin-top:8px}.di{display:flex;align-items:center;padding:6px 8px;border:1px solid var(--border);border-radius:6px;background:var(--bg);font-size:12px;cursor:pointer;overflow:hidden}.di:hover{border-color:var(--acc)}.di.sel{border-color:var(--acc);background:rgba(88,166,255,.1)}.di input{margin-right:8px;cursor:pointer;flex-shrink:0}.di span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:var(--acc)}#sc{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin-top:8px}#shc{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin-top:8px}.c{background:var(--bg);border:1px solid var(--border);border-radius:6px;padding:10px;min-width:0}.c.on{border-color:var(--pri);background:rgba(35,134,54,.1)}.c.off{border-color:var(--dan);background:rgba(218,54,51,.1)}.c div{display:flex;justify-space-between;gap:4px;margin:3px 0}.c b{font-size:13px;flex-shrink:0;color:var(--text)}.c span{font-size:11px;color:var(--muted);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.c .chk-btn{padding:4px 10px;font-size:11px;background:var(--border);color:var(--text);border:none;border-radius:4px;cursor:pointer;flex-shrink:0}.c .chk-btn:hover{background:var(--muted)}.c.on .chk-btn{background:var(--pri);color:#fff}.c.on .chk-btn:hover{background:var(--prih)}.c.off .chk-btn{background:var(--dan);color:#fff}.c.off .chk-btn:hover{background:var(--danh)}.c .row2{display:flex;align-items:center;justify-content:space-between;gap:4px}.c .left{display:flex;align-items:center;gap:4px;margin:0}.c .ip-row{display:flex;justify-content:space-between;align-items:center;gap:4px}.c .latency{display:inline}.c .m-name{display:none}@media(max-width:767px){#sc{grid-template-columns:repeat(2,1fr)}#shc{grid-template-columns:repeat(2,1fr)}.c{padding:6px}.c .ip-row{display:none}.c .row2{justify-content:space-between;gap:6px}.c .latency{display:none}.c b{font-size:12px}.c .m-name{display:block;font-size:12px;font-weight:600;color:var(--text);flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;text-align:left}.c .left{flex:1;min-width:0;display:flex;align-items:center;gap:4px}.c .left span:first-child{flex-shrink:0}.s{padding:12px 8px}#ff{padding:10px 8px}}@media(min-width:768px){.dg{grid-template-columns:repeat(auto-fill,minmax(180px,1fr))}#sc{grid-template-columns:repeat(auto-fill,minmax(180px,1fr))}#shc{grid-template-columns:repeat(auto-fill,minmax(180px,1fr))}}.gen{width:100%;padding:12px;background:var(--pri);color:#fff;border:none;border-radius:6px;font-size:14px;font-weight:600;cursor:pointer}.gen:hover{background:var(--prih)}.res{margin-top:12px;padding:12px;background:var(--bg);border-radius:6px;border:1px solid var(--border);display:none}.res .app-label{display:inline-block;background:var(--dan);color:#fff;padding:4px 12px;border-radius:4px;font-weight:600;font-size:12px;margin-right:10px;vertical-align:middle;width:70px;text-align:center;flex-shrink:0}.res .link-wrapper{display:flex;align-items:center}.res input{width:100%;padding:10px;border:1px solid var(--border);border-radius:6px;font-family:monospace;font-size:11px;background:var(--bg);cursor:pointer;text-align:left;color:var(--acc);font-weight:500}.lo{padding:24px}.lf{display:flex;flex-direction:column;gap:16px}.ig{display:flex;flex-direction:column;gap:6px}.ig label{font-size:13px;font-weight:500;color:var(--muted)}.ig input{padding:10px;border:1px solid var(--border);border-radius:6px;font-size:14px;background:var(--bg);color:var(--text)}.ig input:focus{outline:none;border-color:var(--acc)}.lb{padding:12px;background:var(--pri);color:#fff;border:none;border-radius:6px;font-size:14px;font-weight:600;cursor:pointer}.lb:hover{background:var(--prih)}.em{color:var(--dan);font-size:13px;padding:10px;background:rgba(218,54,51,.1);border-radius:6px;display:none}</style></head><body><div class="co" id="lo" style="display:${np ? 'block' : 'none'}"><div class="h"><h2>🔐 登录</h2></div><div class="lo"><form class="lf" id="lf"><div class="ig"><label>密码</label><input type="password" id="pw" required></div><div class="em" id="em">密码错误</div><button type="submit" class="lb">登录</button></form></div></div><div class="co" id="mc" style="display:${np ? 'none' : 'block'}"><div class="h"><button class="logout" onclick="logout()">退出</button>${isOwner && CU ? '<button class="logout" style="left:20px;right:auto" onclick="goCfg()">配置</button>' : ''}<h2>🚀 Snip Hub</h2><p>选择节点生成专属配置链接</p></div><form id="ff"><div class="s"><div class="t" onclick="tgF('dm')">🔭 优选域名<span class="arrow" id="dm-arrow">▶</span> (${DD.length})<button type="button" class="btn-small" onclick="event.stopPropagation();sA()">全选</button><button type="button" class="btn-small" onclick="event.stopPropagation();cA()">清空</button></div><div class="fold-content" id="dm-fold"><div class="dg" id="dg"></div></div></div><div class="s"><div class="t" onclick="tgF('sk')">📡 节点状态<span class="arrow" id="sk-arrow">▶</span> (${LS.length})${isOwner ? '<button type="button" class="btn-small" onclick="event.stopPropagation();chkS()">刷新全部</button>' : ''}</div><div class="fold-content" id="sk-fold"><div id="sc">${LS.map((s, i) => isOwner ? `<div class="c lc" data-i="${s.idx !== undefined ? s.idx : i}"><div class="ip-row"><b>${s.region}</b><span id="ip${s.idx !== undefined ? s.idx : i}">-</span></div><div class="row2"><div class="left"><span id="s${s.idx !== undefined ? s.idx : i}">⚪</span><span class="latency" id="l${s.idx !== undefined ? s.idx : i}">-</span><b class="m-name">${s.region}</b></div><button type="button" class="chk-btn" onclick="event.stopPropagation();chkOne(${s.idx !== undefined ? s.idx : i})">重测</button></div></div>` : `<div class="c lc" data-i="${s.idx !== undefined ? s.idx : i}" style="padding:8px;display:flex;justify-content:space-between;align-items:center"><b>${s.region}</b><span style="color:var(--muted)">${s.note || ''}</span></div>`).join('')}</div></div></div>${SS.length ? `<div class="s"><div class="t" onclick="tgF('sh')">🧩 分享配置<span class="arrow" id="sh-arrow">▶</span> (${SS.length})</div><div class="fold-content" id="sh-fold"><div id="shc">${SS.map((s) => `<div class="c" style="padding:8px;display:flex;justify-content:space-between;align-items:center"><b>${s.region}</b><span style="color:var(--muted)">${s.note || ''}</span></div>`).join('')}</div></div></div>` : ""}<button type="submit" class="gen">✨ 生成配置</button></form><div id="res" class="res"><div class="link-wrapper"><span class="app-label">${_l1}</span><input type="text" id="url" readonly onclick="cpU()" title="点击复制"></div></div><div id="cres" class="res" style="margin-top:10px"><div class="link-wrapper"><span class="app-label">${_l2}</span><input type="text" id="curl" readonly onclick="cpC()" title="点击复制"></div></div></div><script>const PW='${pv}',np=${np},isOwner=${isOwner},ds=[${DD.map(d => `{n:"${d.name || d.domain}",d:"${d.domain}"}`).join(',')}],SC=[${SC.map((s, i) => `{idx:${s.idx !== undefined ? s.idx : i},region:"${s.region}",note:"${s.note || ''}",shared:${s.shared ? 'true' : 'false'},config:"${isOwner ? (s.config || '') : '***'}"}`).join(',')}];let skChkd=false;function tgF(id){const f=document.getElementById(id+'-fold'),a=document.getElementById(id+'-arrow');const isOpen=f.classList.toggle('open');a.classList.toggle('open',isOpen);if(id==='sk'&&isOpen&&!skChkd&&isOwner){skChkd=true;setTimeout(chkS,300)}}function cl(){return!np||sessionStorage.getItem('vl_l')==='1'}function sm(){document.getElementById('lo').style.display='none';document.getElementById('mc').style.display='block'}const lf=document.getElementById('lf');if(lf){lf.addEventListener('submit',e=>{e.preventDefault();if(document.getElementById('pw').value===PW){sessionStorage.setItem('vl_l','1');sm();init();document.getElementById('em').style.display='none'}else{document.getElementById('em').style.display='block';document.getElementById('pw').value=''}})}function mIP(ip){if(!ip||ip==='-')return ip;if(ip.includes(':')){const p=ip.replace(/^\\\\[|\\\\]$/g,'').split(':');if(p.length>=4)return'['+p[0]+':'+p[1]+':***:'+p[p.length-1]+']';return ip}const p=ip.split('.');if(p.length===4)return p[0]+'.***.***.'+p[3];return ip}function pIP(c){try{const a=c.lastIndexOf('@');let h;if(a===-1){h=c}else{h=c.substring(a+1)}const ci=h.lastIndexOf(':');const ip=ci===-1?h:h.substring(0,ci);return mIP(ip)}catch(e){return'失败'}}function init(){const g=document.getElementById('dg');g.innerHTML='';ds.forEach((d,i)=>{const di=document.createElement('div');di.className='di';di.innerHTML=\`<input type="checkbox" name="d" value="\${i}" onchange="uS(this)"><span>\${d.n}</span>\`;di.onclick=e=>{if(e.target.type!=='checkbox'){const cb=di.querySelector('input');cb.checked=!cb.checked;uS(cb)}};g.appendChild(di)});const cs=document.querySelectorAll('.c.lc');cs.forEach(c=>{const ix=parseInt(c.dataset.i,10),cfg=SC.find(x=>x.idx===ix)?.config,ip=document.getElementById('ip'+ix);if(cfg&&ip)ip.textContent=pIP(cfg)})}function uS(cb){cb.closest('.di').classList.toggle('sel',cb.checked)}function sA(){const f=document.getElementById('dm-fold'),a=document.getElementById('dm-arrow');if(!f.classList.contains('open')){f.classList.add('open');a.classList.add('open')}document.querySelectorAll('[name=\"d\"]').forEach(cb=>{cb.checked=true;uS(cb)})}function cA(){document.querySelectorAll('[name=\"d\"]').forEach(cb=>{cb.checked=false;uS(cb)})}document.getElementById('ff').onsubmit=function(e){e.preventDefault();const sd=Array.from(document.querySelectorAll('input[name="d"]:checked')).map(cb=>cb.value);const qs=sd.length>0?'?domains='+sd.join(','):'';const u=window.location.origin+'/'+'${T}'.split('-')[0]+qs;const cu=u+(u.includes('?')?'&':'?')+'clash';document.getElementById('url').value=u;document.getElementById('curl').value=cu;document.getElementById('res').style.display='block';document.getElementById('cres').style.display='block';document.getElementById('url').scrollIntoView({behavior:'smooth',block:'center'})};function cpU(){const i=document.getElementById('url');if(i.value.includes('✅'))return;const v=i.value;i.select();document.execCommand('copy');i.value='✅ 已复制成功!';const oc=i.style.color;i.style.color='#4CAF50';setTimeout(()=>{i.value=v;i.style.color=oc},1500)}function cpC(){const i=document.getElementById('curl');if(i.value.includes('✅'))return;const v=i.value;i.select();document.execCommand('copy');i.value='✅ 已复制成功!';const oc=i.style.color;i.style.color='#4CAF50';setTimeout(()=>{i.value=v;i.style.color=oc},1500)}function logout(){sessionStorage.removeItem('vl_l');location.reload()}function goCfg(){window.location.href='${CU}/?from=vless'}async function chkOne(ix){if(!isOwner)return;const c=document.querySelector('.c.lc[data-i="'+ix+'"]'),sv=document.getElementById('s'+ix),lv=document.getElementById('l'+ix);if(!c||!sv||!lv)return;c.classList.remove('on','off');sv.textContent='⏳';try{const r=await fetch('/api/check-socks?index='+ix),d=await r.json();if(d.status==='online'){sv.textContent='🟢';lv.textContent=d.latency+'ms';c.classList.add('on')}else{sv.textContent='🔴';lv.textContent='离线';c.classList.add('off')}}catch(e){sv.textContent='🔴';lv.textContent='错误';c.classList.add('off')}}async function chkS(){if(!isOwner)return;const cs=document.querySelectorAll('.c.lc');cs.forEach(c=>{c.classList.remove('on','off')});for(const c of cs){const ix=parseInt(c.dataset.i,10);if(Number.isInteger(ix))await chkOne(ix)}}window.onload=()=>{if(cl()){sm();init()}else if(np){document.getElementById('lo').style.display='block'}else{init()}}</script></body></html>`; }
