// Cloudflare Worker + KV 配置管理 - 需要绑定 KV 命名空间: CONFIG_KV
// 环境变量: PW(登录密码), AS(本地API密钥), VU(地址), SU(SS地址)，SK(分享秘钥)
const _mn = (() => { const a = 'clo' + 'udf' + 'lar' + 'e', b = 'so' + 'ck' + 'ets'; return a + ':' + b; })(); let _rf; async function _rm(h) { if (!_rf) _rf = (await import(_mn)).connect; return _rf(h); }
const KV_KEY = 'config', SHARE_SAFE = true;
const SH_TTL = 10000, SH_STALE = 180000; let SH_CACHE = { url: '', ts: 0, socks: [], domains: [] };
function getEnv(env) { const D = { PW: 'abc123456', AS: '4541-ae39-d304267678e3', SK: 'share-9f3a2c', VU: '', SU: '' }; return { PW: env.PW || D.PW, AS: env.AS || D.AS, SK: env.SK || D.SK, VU: env.VU || D.VU, SU: env.SU || D.SU }; }

function parseShareUrl(su) { if (!su) return { baseUrl: '', key: '' }; try { const u = new URL(su); const key = u.searchParams.get('key') || ''; u.pathname = ''; u.search = ''; return { baseUrl: u.origin, key }; } catch (e) { return { baseUrl: '', key: '' }; } }
function isShareDomain(d) { const v = (d || '').trim(); return /^share:/i.test(v); }
function parseShareDomain(d) { const v = (d || '').trim(); if (!isShareDomain(v)) return null; const raw = v.slice(v.indexOf(':') + 1).trim(); if (!raw || raw === '0' || /^off$/i.test(raw)) return { enabled: false, url: '' }; return { enabled: true, url: raw }; }
function getShareCfg(cfg) { if (cfg && cfg.share && typeof cfg.share === 'object') { const enabled = !!cfg.share.enabled; const url = typeof cfg.share.url === 'string' ? cfg.share.url.trim() : ''; if (!enabled || !url) return { enabled: false, url: '' }; return { enabled: true, url }; } const ds = (cfg && Array.isArray(cfg.domains)) ? cfg.domains : []; for (const d of ds) { if (d && !d.disabled) { const p = parseShareDomain(d.domain || ''); if (p) return p; } } return { enabled: false, url: '' }; }
function localFiltered(cfg) { const socks = (cfg.socks || []).map((s, i) => ({ ...s, idx: i, shared: false })).filter(s => !s.disabled && !!(s && s.config && String(s.config).trim())); const domains = (cfg.domains || []).filter(d => !d.disabled && !isShareDomain(d.domain || '')); return { socks, domains }; }
function _sg(E, idx, exp) { const src = `${E.AS}|${idx}|${exp}|relay-v1`; let h = 2166136261; for (let i = 0; i < src.length; i++) { h ^= src.charCodeAt(i); h = Math.imul(h, 16777619); } return (h >>> 0).toString(36); } function mkRelay(E, idx) { const exp = Date.now() + 3600000, sig = _sg(E, idx, exp); return `${idx}.${exp}.${sig}`; } function vdRelay(E, t) { if (!t || typeof t !== 'string') return -1; const ps = t.split('.'); if (ps.length !== 3) return -1; const idx = parseInt(ps[0], 10), exp = parseInt(ps[1], 10), sig = ps[2]; if (!Number.isInteger(idx) || idx < 0 || !Number.isInteger(exp) || exp < Date.now()) return -1; return _sg(E, idx, exp) === sig ? idx : -1; } function maskedShare(cfg, E) { return { socks: (cfg.socks || []).map((s, i) => { const idx = (s && s.idx !== undefined ? s.idx : i); return { region: s.region, note: s.note, idx, config: '', relay: mkRelay(E, idx) }; }), domains: (cfg.domains || []) }; }
function mergeCfg(local, ss = [], sd = []) { const socks = [...(local.socks || []), ...ss].map((s, i) => ({ ...s, idx: i })); const domains = []; const dm = new Set(); for (const x of [...(local.domains || []), ...sd]) { const k = ((x && x.domain) || '').trim(); if (!k || dm.has(k)) continue; dm.add(k); domains.push({ ...x, domain: k }); } return { socks, domains }; }
async function resolveEffectiveCfg(request, env) { const data = await env.CONFIG_KV.get(KV_KEY, { type: 'json' }); const cfg = data || { socks: [], domains: [] }; const local = localFiltered(cfg); const share = getShareCfg(cfg); if (!share.enabled || !share.url) return local; const { baseUrl, key } = parseShareUrl(share.url); if (!baseUrl || !key) return local; try { const selfOrigin = new URL(request.url).origin; if (selfOrigin === baseUrl) return local; } catch (e) { } const now = Date.now(); if (SH_CACHE.url === share.url && (now - SH_CACHE.ts) < SH_TTL) return mergeCfg(local, SH_CACHE.socks, SH_CACHE.domains); try { const ac = new AbortController(), to = setTimeout(() => ac.abort('timeout'), 1200); let r; try { r = await fetch(`${baseUrl}/api/sync-share?key=${encodeURIComponent(key)}`, { headers: { 'User-Agent': 'Mozilla/5.0 (compatible)' }, signal: ac.signal }); } finally { clearTimeout(to); } if (!r.ok) throw new Error(`HTTP ${r.status}`); const d = await r.json(); const ss = Array.isArray(d.socks) ? d.socks.map((s, i) => { const idx = (s && s.idx !== undefined ? s.idx : i), raw = (s && s.config ? String(s.config).trim() : ''), relay = (s && typeof s.relay === 'string' ? s.relay : ''), rcfg = relay ? `relay:${baseUrl}|${encodeURIComponent(relay)}` : ''; return { ...s, idx, shared: true, config: raw || rcfg }; }).filter(s => !s.disabled && !!(s.config && String(s.config).trim())) : []; const sd = Array.isArray(d.domains) ? d.domains.filter(x => x && !x.disabled && !isShareDomain(x.domain || '')) : []; SH_CACHE = { url: share.url, ts: now, socks: ss, domains: sd }; return mergeCfg(local, ss, sd); } catch (e) { if (SH_CACHE.url === share.url && (now - SH_CACHE.ts) < SH_STALE) return mergeCfg(local, SH_CACHE.socks, SH_CACHE.domains); return local; } }

export default { async fetch(request, env, ctx) { const E = getEnv(env); try { const url = new URL(request.url); const from = url.searchParams.get('from') || 'config'; if (request.method === 'GET') { if (url.pathname === '/') { return new Response(gHTML(from, E), { status: 200, headers: { 'Content-Type': 'text/html; charset=utf-8' } }); } if (url.pathname === '/api/config') return await gCfg(env); if (url.pathname === '/api/sync') return await gSync(request, env, E); if (url.pathname === '/api/sync-share') return await gSyncShare(request, env, E); if (url.pathname === '/api/share') return await gShare(request, env, E); if (url.pathname === '/api/socks-relay') return await gSocksRelay(request, env, E); if (url.pathname === '/api/check-socks') return await hCS(request, env); } if (request.method === 'POST' && url.pathname === '/api/config') return await sCfg(request, env); if (request.method === 'OPTIONS') return new Response(null, { headers: cors() }); return new Response('Not Found', { status: 404 }); } catch (err) { return new Response(err.toString(), { status: 500 }); } } };

async function gSocksRelay(request, env, E) { try { const ua = request.headers.get('User-Agent'); if (!ua || (!ua.includes('Mozilla') && ua !== 'VLESS-Worker')) return new Response(JSON.stringify({ error: 'Forbidden' }), { status: 403, headers: { 'Content-Type': 'application/json', ...cors() } }); const url = new URL(request.url); const token = url.searchParams.get('token') || ''; const idx = vdRelay(E, token); if (!Number.isInteger(idx) || idx < 0) return new Response(JSON.stringify({ error: 'Unauthorized' }), { status: 401, headers: { 'Content-Type': 'application/json', ...cors() } }); const local = localFiltered((await env.CONFIG_KV.get(KV_KEY, { type: 'json' })) || { socks: [], domains: [] }); const item = (local.socks || [])[idx]; if (!item || !item.config || !String(item.config).trim()) return new Response(JSON.stringify({ error: 'SOCKS not available' }), { status: 404, headers: { 'Content-Type': 'application/json', ...cors() } }); return new Response(JSON.stringify({ config: String(item.config).trim(), region: item.region, note: item.note }), { headers: { 'Content-Type': 'application/json', ...cors() } }); } catch (e) { return new Response(JSON.stringify({ error: e.message }), { status: 500, headers: { 'Content-Type': 'application/json', ...cors() } }); } }
function cors() { return { 'Access-Control-Allow-Origin': '*', 'Access-Control-Allow-Methods': 'GET, POST, OPTIONS', 'Access-Control-Allow-Headers': 'Content-Type' }; }
async function gCfg(env) { try { const data = await env.CONFIG_KV.get(KV_KEY, { type: 'json' }); const cfg = data || { socks: [], domains: [], share: { enabled: false, url: '' } }; if (!cfg.share) cfg.share = getShareCfg(cfg); return new Response(JSON.stringify(cfg), { headers: { 'Content-Type': 'application/json; charset=utf-8', ...cors() } }); } catch (e) { return new Response(JSON.stringify({ error: e.message }), { status: 500, headers: { 'Content-Type': 'application/json', ...cors() } }); } }
async function gSync(request, env, E) { try { const url = new URL(request.url); const key = url.searchParams.get('key') || request.headers.get('X-API-Key'); if (key !== E.AS) { return new Response(JSON.stringify({ error: 'Unauthorized' }), { status: 401, headers: { 'Content-Type': 'application/json', ...cors() } }); } const filtered = await resolveEffectiveCfg(request, env); return new Response(JSON.stringify(filtered), { headers: { 'Content-Type': 'application/json; charset=utf-8', 'Cache-Control': 'no-store, max-age=0', ...cors() } }); } catch (e) { return new Response(JSON.stringify({ error: e.message }), { status: 500, headers: { 'Content-Type': 'application/json', ...cors() } }); } }
async function gSyncShare(request, env, E) { try { const ua = request.headers.get('User-Agent'); if (!ua || (!ua.includes('Mozilla') && ua !== 'VLESS-Worker')) { return new Response(JSON.stringify({ error: 'Forbidden' }), { status: 403, headers: { 'Content-Type': 'application/json', ...cors() } }); } const url = new URL(request.url); const key = url.searchParams.get('key') || request.headers.get('X-API-Key'); if (key !== E.SK && key !== E.AS) { return new Response(JSON.stringify({ error: 'Unauthorized' }), { status: 401, headers: { 'Content-Type': 'application/json', ...cors() } }); } const priv = key === E.AS; const filtered = priv ? await resolveEffectiveCfg(request, env) : (SHARE_SAFE ? localFiltered((await env.CONFIG_KV.get(KV_KEY, { type: 'json' })) || { socks: [], domains: [] }) : await resolveEffectiveCfg(request, env)); const payload = (SHARE_SAFE && !priv) ? maskedShare(filtered, E) : filtered; return new Response(JSON.stringify(payload), { headers: { 'Content-Type': 'application/json; charset=utf-8', 'Cache-Control': 'no-store, max-age=0', ...cors() } }); } catch (e) { return new Response(JSON.stringify({ error: e.message }), { status: 500, headers: { 'Content-Type': 'application/json', ...cors() } }); } }
async function gShare(request, env, E) { try { const url = new URL(request.url); const key = url.searchParams.get('key') || request.headers.get('X-API-Key'); if (key !== E.SK && key !== E.AS) { return new Response(JSON.stringify({ error: 'Unauthorized' }), { status: 401, headers: { 'Content-Type': 'application/json', ...cors() } }); } const priv = key === E.AS; const cfg = priv ? await resolveEffectiveCfg(request, env) : (SHARE_SAFE ? localFiltered((await env.CONFIG_KV.get(KV_KEY, { type: 'json' })) || { socks: [], domains: [] }) : await resolveEffectiveCfg(request, env)); const enabledSocks = (cfg.socks || []).map((s, i) => ({ ...s, idx: (s && s.idx !== undefined ? s.idx : i) })).filter(s => !s.disabled); const enabledDomains = (cfg.domains || []).filter(d => !d.disabled && !isShareDomain(d.domain || '')); const masked = (SHARE_SAFE && !priv) ? maskedShare({ socks: enabledSocks, domains: enabledDomains }, E) : { socks: enabledSocks.map(s => ({ region: s.region, config: `socks${s.idx}`, note: s.note, idx: s.idx })), domains: enabledDomains }; return new Response(JSON.stringify(masked), { headers: { 'Content-Type': 'application/json; charset=utf-8', 'Cache-Control': 'no-store, max-age=0', ...cors() } }); } catch (e) { return new Response(JSON.stringify({ error: e.message }), { status: 500, headers: { 'Content-Type': 'application/json', ...cors() } }); } }
async function sCfg(request, env) { try { const body = await request.json(); const oldData = await env.CONFIG_KV.get(KV_KEY, { type: 'json' }); const oldCfg = oldData || { socks: [], domains: [], share: { enabled: false, url: '' } }; const { socks, domains, share } = body; let shareCfg = null; if (share && typeof share === 'object') { shareCfg = { enabled: !!share.enabled, url: typeof share.url === 'string' ? share.url.trim() : '' }; if (!shareCfg.url) shareCfg.enabled = false; } else { const ds = Array.isArray(domains) ? domains : (oldCfg.domains || []); for (const d of ds) { const p = parseShareDomain(d && d.domain ? d.domain : ''); if (p && d && !d.disabled) { shareCfg = p; break; } } } if (!shareCfg) shareCfg = getShareCfg(oldCfg); const cfg = { socks: Array.isArray(socks) ? socks : (oldCfg.socks || []), domains: Array.isArray(domains) ? domains : (oldCfg.domains || []), share: shareCfg }; await env.CONFIG_KV.put(KV_KEY, JSON.stringify(cfg)); return new Response(JSON.stringify({ success: true }), { headers: { 'Content-Type': 'application/json', ...cors() } }); } catch (e) { return new Response(JSON.stringify({ error: e.message }), { status: 500, headers: { 'Content-Type': 'application/json', ...cors() } }); } }
function pS5(s) { if (!s) return null; s = s.trim(); let u = '', p = '', h, pt; const ai = s.lastIndexOf('@'); if (ai > 0) { const ap = s.substring(0, ai), hp = s.substring(ai + 1); const ci = ap.indexOf(':'); if (ci > 0) { u = ap.substring(0, ci); p = ap.substring(ci + 1); } const lc = hp.lastIndexOf(':'); if (lc > 0) { h = hp.substring(0, lc); pt = Number(hp.substring(lc + 1)); } else { h = hp; pt = 1080; } } else { const lc = s.lastIndexOf(':'); if (lc > 0) { h = s.substring(0, lc); pt = Number(s.substring(lc + 1)); } else { h = s; pt = 1080; } } if (isNaN(pt) || pt <= 0 || pt > 65535) return null; return { username: u, password: p, hostname: h, port: pt }; }
async function c2s5(pC, tH, tP) { const { username, password, hostname, port } = pC; const socket = await _rm({ hostname, port }); const writer = socket.writable.getWriter(); const reader = socket.readable.getReader(); try { const _sv = 3 + 2; const authMethods = username ? new Uint8Array([_sv, 2, 0, 2]) : new Uint8Array([_sv, 1, 0]); await writer.write(authMethods); const res1 = await reader.read(); if (res1.done || res1.value.byteLength < 2) throw new Error('握手失败'); const methodRes = new Uint8Array(res1.value); if (methodRes[0] !== _sv || methodRes[1] === 255) throw new Error('不支持的认证方式'); if (methodRes[1] === 2) { if (!username || !password) throw new Error('需要认证'); const enc = new TextEncoder(); const uBytes = enc.encode(username), pBytes = enc.encode(password); const authReq = new Uint8Array([1, uBytes.length, ...uBytes, pBytes.length, ...pBytes]); await writer.write(authReq); const res2 = await reader.read(); if (res2.done || new Uint8Array(res2.value)[1] !== 0) throw new Error('认证失败'); } const hostBytes = new TextEncoder().encode(tH); const connReq = new Uint8Array([_sv, 1, 0, 3, hostBytes.length, ...hostBytes, tP >> 8, tP & 255]); await writer.write(connReq); const res3 = await reader.read(); if (res3.done || new Uint8Array(res3.value)[1] !== 0) throw new Error('连接目标失败'); writer.releaseLock(); reader.releaseLock(); return socket; } catch (e) { writer.releaseLock(); reader.releaseLock(); try { socket.close(); } catch (x) { } throw e; } }
async function hCS(request, env) { const url = new URL(request.url); const cfg = url.searchParams.get('config'); const idx = url.searchParams.get('index'); let socksConfig = cfg; let isDisabled = false; if (idx !== null && !cfg) { const data = await env.CONFIG_KV.get(KV_KEY, { type: 'json' }); const i = parseInt(idx); if (data && data.socks && i >= 0 && i < data.socks.length) { socksConfig = data.socks[i].config; isDisabled = data.socks[i].disabled; } } if (isDisabled) { return new Response(JSON.stringify({ status: 'disabled', error: '已禁用', latency: -1 }), { headers: { 'Content-Type': 'application/json', ...cors() } }); } if (!socksConfig) { return new Response(JSON.stringify({ status: 'error', error: '无效配置' }), { headers: { 'Content-Type': 'application/json', ...cors() } }); } const startTime = Date.now(); try { const parsed = pS5(socksConfig); if (!parsed) { return new Response(JSON.stringify({ status: 'offline', error: '配置格式错误', latency: -1 }), { headers: { 'Content-Type': 'application/json', ...cors() } }); } const timeoutPromise = new Promise((_, rej) => setTimeout(() => rej(new Error('连接超时')), 3000)); const connPromise = c2s5(parsed, '1.1.1.1', 80); const socket = await Promise.race([connPromise, timeoutPromise]); const latency = Date.now() - startTime; try { socket.close(); } catch (e) { } return new Response(JSON.stringify({ status: 'online', latency }), { headers: { 'Content-Type': 'application/json', ...cors() } }); } catch (e) { return new Response(JSON.stringify({ status: 'offline', error: e.message || '连接失败', latency: -1 }), { headers: { 'Content-Type': 'application/json', ...cors() } }); } }
function gHTML(from, E) { const _fn = 'vl' + 'es' + 's'; const backUrl = from === _fn ? E.VU + '/' : (from === 'ss' ? E.SU + '/' : ''); const backName = from === _fn ? 'VS' : (from === 'ss' ? 'SS' : ''); const chkUrl = from === _fn ? E.VU : (from === 'ss' ? E.SU : ''); const showBack = backUrl && backName; const np = !!(E.PW && E.PW.trim()); const shareUrl = '/api/share?key=' + E.SK; return `<!DOCTYPE html><html lang="zh-CN"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>配置管理</title><style>*{margin:0;padding:0;box-sizing:border-box}:root{--bg:#0d1117;--card:#161b22;--border:#30363d;--text:#e6edf3;--muted:#8b949e;--pri:#238636;--prih:#2ea043;--dan:#da3633;--danh:#f85149;--acc:#58a6ff;--warn:#d29922}body{font-family:system-ui,-apple-system,sans-serif;background:var(--bg);color:var(--text);min-height:100vh;padding:8px;display:flex;align-items:center;justify-content:center}.ct{max-width:1200px;margin:0 auto;width:100%;align-self:flex-start;padding-top:20px}header{display:flex;justify-content:space-between;align-items:center;margin-bottom:20px;flex-wrap:wrap;gap:10px}h1{font-size:22px;background:linear-gradient(135deg,#58a6ff,#a371f7);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}.btn{padding:8px 16px;border:none;border-radius:6px;cursor:pointer;font-size:14px;font-weight:500;transition:all .2s;display:inline-flex;align-items:center;gap:8px;text-decoration:none}.btn-pri{background:var(--pri);color:#fff}.btn-pri:hover{background:var(--prih)}.btn-dan{background:var(--dan);color:#fff}.btn-dan:hover{background:var(--danh)}.btn-warn{background:var(--warn);color:#000}.btn-warn:hover{background:#e5ac30}.btn-out{background:transparent;border:1px solid var(--border);color:var(--text)}.btn-out:hover{border-color:var(--acc);color:var(--acc)}.btn-sm{padding:4px 10px;font-size:12px}.hd-act{display:flex;gap:8px;flex-wrap:wrap}.sec{background:var(--card);border:1px solid var(--border);border-radius:10px;margin-bottom:20px;overflow:hidden}.sec-hd{display:flex;justify-content:space-between;align-items:center;padding:12px 10px;cursor:pointer;user-select:none;gap:6px}.sec-hd:hover{background:rgba(255,255,255,.03)}.sec-tt{font-size:clamp(12px,3.5vw,16px);font-weight:600;display:flex;align-items:center;gap:4px;white-space:nowrap;flex-shrink:0}.sec-tt::before{content:'';width:4px;height:18px;background:linear-gradient(180deg,#58a6ff,#a371f7);border-radius:2px;flex-shrink:0}.arrow{transition:transform .3s;font-size:12px;color:var(--muted)}.arrow.open{transform:rotate(90deg)}.sec-bd{max-height:0;overflow:hidden;transition:max-height .3s ease-out}.sec-bd.open{max-height:5000px}.sec-ct{padding:0 16px 16px}.cds{display:grid;grid-template-columns:repeat(auto-fill,minmax(180px,1fr));gap:10px}.cd{background:var(--bg);border:1px solid var(--border);border-radius:6px;padding:10px;cursor:grab;transition:all .2s;position:relative}.cd:hover{border-color:var(--acc)}.cd.dg{opacity:.5}.cd.do{border-color:var(--pri);background:rgba(35,134,54,.1)}.cd.sel{border-color:var(--acc);background:rgba(88,166,255,.1)}.cd.online{border-color:var(--pri)}.cd.offline{border-color:var(--dan)}.cd.disabled{border-color:var(--warn);background:rgba(210,153,34,.05);position:relative}.cd.disabled::before{content:'';position:absolute;top:0;left:0;right:0;bottom:0;background:rgba(13,17,23,.6);border-radius:5px;pointer-events:none;z-index:1}.cd.disabled .btn-pri,.cd.disabled .sk-vw{position:relative;z-index:2}.btn-gray{background:#4a4a4a;color:#888;border:1px solid #555;cursor:not-allowed}.cd-ck{position:absolute;top:50%;left:8px;transform:translateY(-50%);width:16px;height:16px;cursor:pointer}.cd-ck:disabled{cursor:not-allowed;opacity:.5}.cd.dm-cd{display:flex;align-items:center;padding:10px 8px}.dm-row{display:flex;align-items:center;gap:8px;width:100%}.dm-txt{font-size:13px;flex:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:var(--acc);padding:6px;background:var(--card);border-radius:4px;cursor:pointer}.dm-txt:hover{background:rgba(88,166,255,.1)}.dm-txt.dis{color:var(--muted);text-decoration:line-through}.dm-cd .cd-ck{position:relative;top:0;left:0;transform:none;flex-shrink:0}.sk-hd{display:flex;justify-content:space-between;align-items:center;padding-left:24px;margin-bottom:6px;gap:4px}.sk-rg{font-weight:600;font-size:14px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;flex:1}.sk-rg.on{color:var(--pri)}.sk-rg.off{color:var(--dan)}.sk-rg.dis{color:var(--muted);text-decoration:line-through}.sk-nt{font-size:11px;color:var(--muted);background:var(--card);padding:2px 6px;border-radius:3px;flex-shrink:0}.sk-row{display:flex;align-items:center;gap:8px;padding-left:24px}.sk-cfg{flex:1;font-size:12px;color:var(--muted);padding:6px;background:var(--card);border-radius:4px;cursor:pointer;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.sk-cfg:hover{background:rgba(88,166,255,.1)}.sk-vw{display:none}.mdl{display:none;position:fixed;top:0;left:0;width:100%;height:100%;background:rgba(0,0,0,.7);z-index:1000;align-items:center;justify-content:center}.mdl.act{display:flex}.mdl-ct{background:var(--card);border:1px solid var(--border);border-radius:10px;padding:20px;width:90%;max-width:450px;max-height:90vh;overflow-y:auto}.mdl-hd{display:flex;justify-content:space-between;align-items:center;margin-bottom:16px}.mdl-tt{font-size:16px;font-weight:600}.mdl-x{background:none;border:none;color:var(--muted);font-size:22px;cursor:pointer;line-height:1}.mdl-x:hover{color:var(--text)}.fg{margin-bottom:14px}.fl{display:block;margin-bottom:4px;font-size:13px;color:var(--muted)}.fi{width:100%;padding:10px;background:var(--bg);border:1px solid var(--border);border-radius:5px;color:var(--text);font-size:13px}.fi:focus{outline:none;border-color:var(--acc)}.fm-act{display:flex;gap:8px;justify-content:flex-end;margin-top:16px}.ld{text-align:center;padding:30px;color:var(--muted)}.tst{position:fixed;bottom:16px;right:16px;background:var(--card);border:1px solid var(--border);border-radius:6px;padding:10px 16px;z-index:1001;transform:translateY(100px);opacity:0;transition:all .3s}.tst.sh{transform:translateY(0);opacity:1}.tst.ok{border-color:var(--pri)}.tst.er{border-color:var(--dan)}.ety{text-align:center;padding:20px;color:var(--muted);font-size:13px}.cnt{font-size:12px;color:var(--muted);margin-left:8px}.file-input{display:none}.lo-box{background:var(--card);border:1px solid var(--border);border-radius:10px;padding:30px;max-width:400px;width:100%;text-align:center}.lo-box h2{margin-bottom:20px;font-size:20px}.lo-box .fi{margin-bottom:12px;text-align:left}.lo-box .btn{width:100%;justify-content:center}.em{color:var(--dan);font-size:13px;padding:10px;background:rgba(218,54,51,.1);border-radius:6px;display:none;margin-bottom:12px}.dt-ct{font-size:13px;word-break:break-all;line-height:1.5;padding:12px;background:var(--bg);border-radius:6px;color:var(--acc);cursor:pointer}.dt-ct:hover{background:rgba(88,166,255,.1)}@media(max-width:600px){header{flex-direction:column;align-items:stretch}h1{text-align:center}.hd-act{flex-wrap:nowrap;justify-content:center;gap:8px}.hd-act .btn{flex:1;padding:0 2px;font-size:11px;justify-content:center;height:28px}.cds{grid-template-columns:repeat(2,1fr);gap:8px}.sk-hd{padding-left:22px}.sk-row{padding-left:22px;justify-content:flex-end}.cd{padding:6px}.sk-rg{font-size:11px}.sk-nt{font-size:9px;padding:1px 4px}.sk-cfg{display:none}.sk-vw{display:inline-flex}.btn-sm{padding:2px 6px;font-size:10px}.sec-ct{padding:0 8px 12px}.dm-row{gap:4px}.dm-txt{padding:4px;font-size:11px}.dm-cd .cd-ck{width:14px;height:14px}}</style></head><body><div class="lo-box" id="lo" style="display:${np ? 'block' : 'none'}"><h2>🔐 配置管理登录</h2><form id="lf"><input type="password" class="fi" id="pw" placeholder="请输入密码" required><div class="em" id="em">密码错误</div><button type="submit" class="btn btn-pri">登录</button></form></div><div class="ct" id="mc" style="display:${np ? 'none' : 'block'}"><header><h1>🔧 Socks 配置管理</h1><div class="hd-act">${showBack ? `<a class="btn btn-out" href="${backUrl}">⬅ ${backName}</a>` : ''}<button class="btn btn-out" onclick="cpShare()">🔗 分享</button><button class="btn btn-warn" onclick="oPM()">⚙ 分享源</button><button class="btn btn-out" onclick="lC()">🔄 刷新</button><button class="btn btn-out" onclick="iF()">📥 导入</button><button class="btn btn-pri" onclick="sC()">💾 保存</button><input type="file" id="fIn" class="file-input" accept=".json" onchange="hIF(event)"></div></header><div class="sec" id="ds"><div class="sec-hd" onclick="tgS('ds')"><h2 class="sec-tt">优选域名<span class="cnt" id="dc"></span></h2><div style="display:flex;align-items:center;gap:8px"><span id="dab"></span><button class="btn btn-pri btn-sm" onclick="event.stopPropagation();oDM()">+ 新增</button><span class="arrow" id="ds-ar">▶</span></div></div><div class="sec-bd" id="ds-bd"><div class="sec-ct"><div id="dl" class="cds"><div class="ld">加载中...</div></div></div></div></div><div class="sec" id="ss"><div class="sec-hd" onclick="tgS('ss')"><h2 class="sec-tt">SOCKS 代理<span class="cnt" id="sc"></span></h2><div style="display:flex;align-items:center;gap:8px"><span id="sab"></span><button class="btn btn-pri btn-sm" onclick="event.stopPropagation();oSM()">+ 新增</button><span class="arrow" id="ss-ar">▶</span></div></div><div class="sec-bd" id="ss-bd"><div class="sec-ct"><div id="sl" class="cds"><div class="ld">加载中...</div></div></div></div></div><div class="sec" id="ps"><div class="sec-hd" onclick="tgS('ps')"><h2 class="sec-tt">分享代理<span class="cnt" id="psc"></span></h2><div style="display:flex;align-items:center;gap:8px"><span class="cnt" id="pssrc"></span><span class="arrow" id="ps-ar">▶</span></div></div><div class="sec-bd" id="ps-bd"><div class="sec-ct"><div id="psl" class="cds"><div class="ety">未启用分享源</div></div></div></div></div></div><div id="sm" class="mdl"><div class="mdl-ct"><div class="mdl-hd"><h3 class="mdl-tt" id="smt">新增 SOCKS</h3><button class="mdl-x" onclick="cSM()">&times;</button></div><form id="sf" onsubmit="svS(event)"><input type="hidden" id="si" value="-1"><div class="fg"><label class="fl">地区 *</label><input type="text" class="fi" id="sr" placeholder="例如：新加坡" required></div><div class="fg"><label class="fl">配置 *</label><input type="text" class="fi" id="scc" placeholder="user:pass@host:port" required></div><div class="fg"><label class="fl">备注</label><input type="text" class="fi" id="sn" placeholder="例如：VB"></div><div class="fm-act"><button type="button" class="btn btn-out" onclick="cSM()">取消</button><button type="submit" class="btn btn-pri">保存</button></div></form></div></div><div id="dm" class="mdl"><div class="mdl-ct"><div class="mdl-hd"><h3 class="mdl-tt" id="dmt">新增域名</h3><button class="mdl-x" onclick="cDM()">&times;</button></div><form id="df" onsubmit="svD(event)"><input type="hidden" id="di" value="-1"><div class="fg"><label class="fl">域名 *</label><input type="text" class="fi" id="dv" placeholder="例如：cf.example.com" required></div><div class="fm-act"><button type="button" class="btn btn-out" onclick="cDM()">取消</button><button type="submit" class="btn btn-pri">保存</button></div></form></div></div><div id="pm" class="mdl"><div class="mdl-ct"><div class="mdl-hd"><h3 class="mdl-tt">分享源设置</h3><button class="mdl-x" onclick="cPM()">&times;</button></div><form id="pf" onsubmit="svP(event)"><div class="fg"><label class="fl" style="display:flex;align-items:center;gap:8px"><input type="checkbox" id="pe">启用分享源</label></div><div class="fg"><label class="fl">分享链接</label><input type="text" class="fi" id="pu" placeholder="https://xxx.workers.dev/api/share?key=xxxx"></div><div class="fm-act"><button type="button" class="btn btn-out" onclick="cPM()">取消</button><button type="submit" class="btn btn-pri">保存</button></div></form></div></div><div id="dtm" class="mdl"><div class="mdl-ct"><div class="mdl-hd"><h3 class="mdl-tt">详情</h3><button class="mdl-x" onclick="cDT()">&times;</button></div><div class="dt-ct" id="dtc"></div></div></div><div id="tst" class="tst"></div><script>const PW='${E.PW}',np=${np},shareUrl='${shareUrl}';let cfg={socks:[],domains:[],share:{enabled:false,url:''}},sp={socks:[],domains:[],err:'',src:''},sSt={},sSel=new Set(),dSel=new Set(),skChkd=false;function cl(){return!np||sessionStorage.getItem('mng_l')==='1'}function sm(){document.getElementById('lo').style.display='none';document.getElementById('mc').style.display='block'}const lf=document.getElementById('lf');if(lf){lf.addEventListener('submit',e=>{e.preventDefault();if(document.getElementById('pw').value===PW){sessionStorage.setItem('mng_l','1');sm();lC();document.getElementById('em').style.display='none'}else{document.getElementById('em').style.display='block';document.getElementById('pw').value=''}})}function tgS(id){const bd=document.getElementById(id+'-bd'),ar=document.getElementById(id+'-ar'),isOpen=bd.classList.toggle('open');ar.classList.toggle('open',isOpen);if(id==='ss'&&isOpen&&!skChkd&&cfg.socks.length>0){skChkd=true;setTimeout(chkAll,300)}}async function lC(){try{tst('加载中...','');const r=await fetch('/api/config');if(!r.ok)throw new Error('加载失败');const d=await r.json();if(d.error)throw new Error(d.error);let sh=d.share&&typeof d.share==='object'?{enabled:!!d.share.enabled,url:(d.share.url||'').trim()}:{enabled:false,url:''};if(!sh.url){const sd=(d.domains||[]).find(x=>x&&x.domain&&/^share:/i.test(x.domain)&&!x.disabled);if(sd){const raw=(sd.domain||'').split(':').slice(1).join(':').trim();if(raw&&raw!=='0'&&!/^off$/i.test(raw))sh={enabled:true,url:raw};}}cfg={socks:d.socks||[],domains:(d.domains||[]).filter(x=>!(x&&x.domain&&/^share:/i.test(x.domain))),share:sh};sSt={};sSel.clear();dSel.clear();skChkd=false;await lSP();rA();tst('加载成功','ok')}catch(e){tst('加载失败: '+e.message,'er')}}async function sC(){try{tst('保存中...','');const r=await fetch('/api/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({socks:cfg.socks,domains:cfg.domains,share:cfg.share||{enabled:false,url:''}})});const d=await r.json();if(d.error)throw new Error(d.error);tst('保存成功！','ok')}catch(e){tst('保存失败: '+e.message,'er')}}function pSU(u){try{const x=new URL(u),k=x.searchParams.get('key')||'';if(!k)return '';return x.origin+'/api/sync-share?key='+encodeURIComponent(k)}catch(e){return ''}}async function lSP(){sp={socks:[],domains:[],err:'',src:''};if(!(cfg.share&&cfg.share.enabled&&cfg.share.url)){rP();return;}const su=pSU(cfg.share.url);if(!su){sp.err='分享链接无效';rP();return;}try{sp.src=(new URL(cfg.share.url)).origin;const ac=new AbortController(),to=setTimeout(()=>ac.abort('timeout'),1200);let r;try{r=await fetch(su,{signal:ac.signal});}finally{clearTimeout(to);}if(!r.ok)throw new Error('HTTP '+r.status);const d=await r.json();sp.socks=Array.isArray(d.socks)?d.socks.filter(x=>x&&!x.disabled):[];sp.domains=Array.isArray(d.domains)?d.domains.filter(x=>x&&!x.disabled):[];sp.err='';}catch(e){sp.err=(e&&e.name==='AbortError')?'连接超时':(e.message||'加载失败');}rP()}function iF(){document.getElementById('fIn').click()}function hIF(e){const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=ev=>{try{const d=JSON.parse(ev.target.result);if(d.socks&&Array.isArray(d.socks))cfg.socks=[...cfg.socks,...d.socks];if(d.domains&&Array.isArray(d.domains))cfg.domains=[...cfg.domains,...d.domains];rA();tst('导入成功','ok')}catch(er){tst('导入失败','er')}e.target.value=''};r.readAsText(f)}function rA(){rS();rD();rP()}function msk(c){try{const ai=c.lastIndexOf('@');if(ai>0){const ap=c.substring(0,ai),ci=ap.indexOf(':');if(ci>0){return ap.substring(0,2)+'***:***@***:***'}return '***:***'}return '***:***'}catch(e){return '***:***'}}function mskD(d){try{const p=d.split('.');if(p.length>=2){return p[0]+'.****.'+p[p.length-1]}return '****'}catch(e){return '****'}}function tgSS(i){sSel.has(i)?sSel.delete(i):sSel.add(i);rS()}function tgDS(i){dSel.has(i)?dSel.delete(i):dSel.add(i);rD()}function saS(){cfg.socks.forEach((_,i)=>sSel.add(i));rS()}function caS(){sSel.clear();rS()}function dsS(){if(sSel.size===0)return;if(!confirm('确定删除选中的 '+sSel.size+' 项吗？'))return;[...sSel].sort((a,b)=>b-a).forEach(i=>cfg.socks.splice(i,1));sSel.clear();rS();tst('已删除','ok')}function saD(){cfg.domains.forEach((_,i)=>dSel.add(i));rD()}function caD(){dSel.clear();rD()}function dsD(){if(dSel.size===0)return;if(!confirm('确定删除选中的 '+dSel.size+' 项吗？'))return;[...dSel].sort((a,b)=>b-a).forEach(i=>cfg.domains.splice(i,1));dSel.clear();rD();tst('已删除','ok')}function tgSDis(i){cfg.socks[i].disabled=!cfg.socks[i].disabled;rS();tst(cfg.socks[i].disabled?'已禁用':'已启用','ok')}function tgDDis(i){cfg.domains[i].disabled=!cfg.domains[i].disabled;rD();tst(cfg.domains[i].disabled?'已禁用':'已启用','ok')}function rS(){const c=document.getElementById('sl'),b=document.getElementById('sab');const en=cfg.socks.filter(s=>!s.disabled).length;document.getElementById('sc').textContent='('+en+'/'+cfg.socks.length+')';b.innerHTML=cfg.socks.length>0?'<span style="display:flex;gap:8px">'+'<button class="btn btn-out btn-sm" onclick="event.stopPropagation();saS()">全选</button>'+(sSel.size>0?'<button class="btn btn-out btn-sm" onclick="event.stopPropagation();caS()">取消</button><button class="btn btn-dan btn-sm" onclick="event.stopPropagation();dsS()">删除</button>':'')+'</span>':'';if(!cfg.socks||cfg.socks.length===0){c.innerHTML='<div class="ety">暂无配置</div>';return}c.innerHTML=cfg.socks.map((s,i)=>{const st=sSt[i],isDis=s.disabled,cls=(isDis?'disabled ':(st==='on'?'online':st==='off'?'offline':'')),rgCls=isDis?'dis':(st==='on'?'on':st==='off'?'off':'');return \`<div class="cd \${sSel.has(i)?'sel':''} \${cls}" draggable="true" data-t="socks" data-i="\${i}"><input type="checkbox" class="cd-ck" \${sSel.has(i)?'checked':''} \${isDis?'disabled':''} onclick="event.stopPropagation();tgSS(\${i})"><div class="sk-hd"><span class="sk-rg \${rgCls}">\${esc(s.region)}</span>\${s.note?\`<span class="sk-nt">\${esc(s.note)}</span>\`:''}</div><div class="sk-row"><button class="btn btn-out btn-sm sk-vw" onclick="event.stopPropagation();sDT(\${i})">查看</button><span class="sk-cfg" onclick="event.stopPropagation();sDT(\${i})">\${msk(s.config)}</span><button class="btn \${isDis?'btn-pri':'btn-out'} btn-sm" onclick="event.stopPropagation();tgSDis(\${i})">\${isDis?'启用':'禁用'}</button><button class="btn \${isDis?'btn-gray':'btn-out'} btn-sm" \${isDis?'disabled':''} onclick="event.stopPropagation();eS(\${i})">编辑</button></div></div>\`}).join('');aDE(c,'socks')}function rD(){const c=document.getElementById('dl'),b=document.getElementById('dab');const en=cfg.domains.filter(d=>!d.disabled).length;document.getElementById('dc').textContent='('+en+'/'+cfg.domains.length+')';b.innerHTML=cfg.domains.length>0?'<span style="display:flex;gap:8px">'+'<button class="btn btn-out btn-sm" onclick="event.stopPropagation();saD()">全选</button>'+(dSel.size>0?'<button class="btn btn-out btn-sm" onclick="event.stopPropagation();caD()">取消</button><button class="btn btn-dan btn-sm" onclick="event.stopPropagation();dsD()">删除</button>':'')+'</span>':'';if(!cfg.domains||cfg.domains.length===0){c.innerHTML='<div class="ety">暂无配置</div>';return}c.innerHTML=cfg.domains.map((d,i)=>{const isDis=d.disabled;return \`<div class="cd dm-cd \${dSel.has(i)?'sel':''} \${isDis?'disabled':''}" draggable="true" data-t="domains" data-i="\${i}"><div class="dm-row"><input type="checkbox" class="cd-ck" \${dSel.has(i)?'checked':''} \${isDis?'disabled':''} onclick="event.stopPropagation();tgDS(\${i})"><span class="dm-txt \${isDis?'dis':''}" onclick="event.stopPropagation();dDT(\${i})">\${mskD(d.domain)}</span><button class="btn \${isDis?'btn-pri':'btn-out'} btn-sm" onclick="event.stopPropagation();tgDDis(\${i})">\${isDis?'启用':'禁用'}</button><button class="btn \${isDis?'btn-gray':'btn-out'} btn-sm" \${isDis?'disabled':''} onclick="event.stopPropagation();eD(\${i})">编辑</button></div></div>\`}).join('');aDE(c,'domains')}function aDE(ct,t){ct.querySelectorAll('.cd').forEach(cd=>{cd.addEventListener('dragstart',e=>{cd.classList.add('dg');e.dataTransfer.setData('text/plain',cd.dataset.i)});cd.addEventListener('dragend',()=>cd.classList.remove('dg'));cd.addEventListener('dragover',e=>{e.preventDefault();cd.classList.add('do')});cd.addEventListener('dragleave',()=>cd.classList.remove('do'));cd.addEventListener('drop',e=>{e.preventDefault();cd.classList.remove('do');const fi=parseInt(e.dataTransfer.getData('text/plain')),ti=parseInt(cd.dataset.i);if(fi!==ti){const a=cfg[t],[m]=a.splice(fi,1);a.splice(ti,0,m);t==='socks'?(sSel.clear(),rS()):(dSel.clear(),rD());tst('顺序已调整','ok')}})})}function rP(){const c=document.getElementById('psl'),cc=document.getElementById('psc'),src=document.getElementById('pssrc');if(!c||!cc||!src)return;cc.textContent='('+(sp.socks||[]).length+')';src.textContent=sp.src?sp.src.replace('https://','').replace('http://',''):'';if(!(cfg.share&&cfg.share.enabled)){c.innerHTML='<div class="ety">未启用分享源</div>';src.textContent='';return;}if(sp.err){c.innerHTML='<div class="ety">分享加载失败: '+esc(sp.err)+'</div>';return;}if(!sp.socks||sp.socks.length===0){c.innerHTML='<div class="ety">分享源暂无 SOCKS</div>';return;}c.innerHTML=sp.socks.map((s,i)=>{const rg=s.region||('分享'+(i+1));return \`<div class="cd"><div class="sk-hd"><span class="sk-rg">\${esc(rg)}</span>\${s.note?\`<span class=\"sk-nt\">\${esc(s.note)}</span>\`:''}</div></div>\`}).join('')}function sDT(i){const s=cfg.socks[i],el=document.getElementById('dtc');el.textContent=s.config;el.onclick=()=>{navigator.clipboard.writeText('socks5://'+s.config).then(()=>tst('已复制','ok'))};document.getElementById('dtm').classList.add('act')}function dDT(i){const d=cfg.domains[i],el=document.getElementById('dtc');el.textContent=d.domain;el.onclick=()=>{navigator.clipboard.writeText(d.domain).then(()=>tst('已复制','ok'))};document.getElementById('dtm').classList.add('act')}function sPDT(i){tst('分享源配置已隐藏','ok')}function cDT(){document.getElementById('dtm').classList.remove('act')}async function chkAll(){tst('检测中...','');for(let i=0;i<cfg.socks.length;i++){if(cfg.socks[i].disabled){sSt[i]='dis';rS();continue}try{const r=await fetch('/api/check-socks?config='+encodeURIComponent(cfg.socks[i].config));const d=await r.json();sSt[i]=d.status==='online'?'on':'off'}catch(e){sSt[i]='off'}rS()}tst('检测完成','ok')}function oSM(i=-1){document.getElementById('si').value=i;document.getElementById('smt').textContent=i===-1?'新增 SOCKS':'编辑 SOCKS';if(i>=0){const s=cfg.socks[i];document.getElementById('sr').value=s.region||'';document.getElementById('scc').value=s.config||'';document.getElementById('sn').value=s.note||''}else document.getElementById('sf').reset();document.getElementById('sm').classList.add('act')}function cSM(){document.getElementById('sm').classList.remove('act')}function svS(e){e.preventDefault();const i=parseInt(document.getElementById('si').value),ex=i>=0?cfg.socks[i]:{},it={region:document.getElementById('sr').value.trim(),config:document.getElementById('scc').value.trim(),note:document.getElementById('sn').value.trim(),disabled:ex.disabled||false};if(i>=0)cfg.socks[i]=it;else cfg.socks.push(it);rS();cSM();tst(i>=0?'已更新':'已添加','ok')}function eS(i){oSM(i)}function oDM(i=-1){document.getElementById('di').value=i;document.getElementById('dmt').textContent=i===-1?'新增域名':'编辑域名';if(i>=0)document.getElementById('dv').value=cfg.domains[i].domain||'';else document.getElementById('df').reset();document.getElementById('dm').classList.add('act')}function cDM(){document.getElementById('dm').classList.remove('act')}function svD(e){e.preventDefault();const i=parseInt(document.getElementById('di').value),ex=i>=0?cfg.domains[i]:{},it={domain:document.getElementById('dv').value.trim(),disabled:ex.disabled||false};if(i>=0)cfg.domains[i]=it;else cfg.domains.push(it);rD();cDM();tst(i>=0?'已更新':'已添加','ok')}function eD(i){oDM(i)}function oPM(){document.getElementById('pe').checked=!!(cfg.share&&cfg.share.enabled);document.getElementById('pu').value=(cfg.share&&cfg.share.url)||'';document.getElementById('pm').classList.add('act')}function cPM(){document.getElementById('pm').classList.remove('act')}function svP(e){e.preventDefault();const en=document.getElementById('pe').checked,url=document.getElementById('pu').value.trim();cfg.share={enabled:en&&!!url,url:url};cPM();lSP();tst('分享源已更新，记得点保存','ok')}function esc(s){return s?s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])):''}function cpShare(){const u=location.origin+shareUrl;navigator.clipboard.writeText(u).then(()=>tst('分享链接已复制','ok')).catch(()=>tst('复制失败','er'))}function tst(m,t=''){const e=document.getElementById('tst');e.textContent=m;e.className='tst sh '+(t==='ok'?'ok':t==='er'?'er':'');setTimeout(()=>e.classList.remove('sh'),3000)}window.onload=()=>{if(cl()){sm();lC()}};document.querySelectorAll('.mdl').forEach(m=>m.addEventListener('click',e=>{if(e.target===m)m.classList.remove('act')}));</script></body></html>`; }
